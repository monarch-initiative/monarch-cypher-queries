query: |
        MATCH path=(pub:publication)<-[:dc:source]-(association:association)-[r:OBAN:association_has_object|OBAN:association_has_subject]->(genotype:genotype)-[:BFO:0000051!*..15]->(variant:`sequence feature`)
        WHERE (NOT variant:anonymous AND NOT variant:gene) OR variant:`reagent targeted gene`
        RETURN DISTINCT path,
            pub as subject,
            variant as object,
            'publication' as subject_category,
            'variant' as object_category,
            'indirect' as qualifier
        UNION
        MATCH path=(pub:publication)-[r:IAO:0000142|IAO:0000136]->(genotype:genotype)-[:BFO:0000051!*..15]->(variant:`sequence feature`)
        WHERE (NOT variant:anonymous AND NOT variant:gene) OR variant:`reagent targeted gene`
        RETURN DISTINCT path,
            pub as subject,
            variant as object,
            'publication' as subject_category,
            'variant' as object_category,
            'inferred' as qualifier
        UNION
        MATCH path=(pub:publication)<-[:dc:source]-(association:association)-[r:OBAN:association_has_subject|OBAN:association_has_object]->(variant:`sequence feature`)
        WHERE (NOT variant:anonymous AND NOT variant:gene) OR variant:`reagent targeted gene`
        RETURN DISTINCT path,
            pub as subject,
            variant as object,
            'publication' as subject_category,
            'variant' as object_category,
            'direct' as qualifier
        UNION
        MATCH path=(pub:publication)-[r:IAO:0000142|IAO:0000136]->(variant:`sequence feature`)
        WHERE (NOT variant:anonymous AND NOT variant:gene) OR variant:`reagent targeted gene`
        RETURN DISTINCT path,
            pub as subject,
            variant as object,
            'publication' as subject_category,
            'variant' as object_category,
            'inferred' as qualifier
        UNION
        MATCH path=(pub:publication)<-[:dc:source]-(association:association)-[r:OBAN:association_has_subject|OBAN:association_has_object]->(person)-[:GENO:0000222|RO:0001000*1..2]->(genotype:genotype)-[:BFO:0000051!*..15]->(variant:`sequence feature`)
        WHERE (NOT variant:anonymous AND NOT variant:gene) OR variant:`reagent targeted gene`
        RETURN DISTINCT path,
            pub as subject,
            variant as object,
            'publication' as subject_category,
            'variant' as object_category,
            'indirect' as qualifier
        UNION
        MATCH path=(pub:publication)-[r:IAO:0000142|IAO:0000136]->(person)-[:GENO:0000222|RO:0001000*1..2]->(genotype:genotype)-[:BFO:0000051!*..15]->(variant:`sequence feature`)
        WHERE (NOT variant:anonymous AND NOT variant:gene) OR variant:`reagent targeted gene`
        RETURN DISTINCT path,
            pub as subject,
            variant as object,
            'publication' as subject_category,
            'variant' as object_category,
            'inferred' as qualifier
